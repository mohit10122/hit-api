name: Hit API Every 9 Minutes

on:
  schedule:
    - cron: '*/9 * * * *'
  workflow_dispatch:

jobs:
  call_api:
    runs-on: ubuntu-latest
    steps:
      - name: Call my API
        run: |
          curl -X POST "https://staffpro.onrender.com/Api/Staff/login"
